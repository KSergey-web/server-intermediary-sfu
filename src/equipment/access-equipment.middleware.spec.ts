import { AccessEquipmentMiddleware } from './access-equipment.middleware';

describe('AccessEquipmentMiddleware', () => {
  it('should be defined', () => {
    expect(new AccessEquipmentMiddleware()).toBeDefined();
  });
});
